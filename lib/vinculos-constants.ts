export const VINCULOS_LOTE_MAX_PDFS = 100

/** Abaixo deste tamanho o ZIP pode ir direto na resposta HTTP (limite Vercel ~4,5 MB). */
export const VINCULOS_LOTE_ZIP_DIRECT_MAX_BYTES = 3.5 * 1024 * 1024
